version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backend-job-notifier
    ports:
      - "8080:8080"
    env_file:
      - .env.docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
